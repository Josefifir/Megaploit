# constraints.txt
# ~~~~~~~~~~~~~~~~
# Tested exact package versions for reproducible installs.
# Generated from requirements.in via:
#   pip-compile requirements.in --output-file constraints.txt
#
# Use alongside requirements.txt:
#   pip install -r requirements.txt -c constraints.txt
#
# Update this file when you bump requirements.in:
#   pip install pip-tools
#   pip-compile requirements.in --output-file constraints.txt
#
# Last verified: 2026-07 against Python 3.10 / 3.11 / 3.12

# ── Core ──────────────────────────────────────────────────────────────────
termcolor==3.3.0
tomli==2.4.1

# ── Agent capabilities ────────────────────────────────────────────────────
pynput==1.8.2
PyAutoGUI==0.9.54
mss==10.2.0
opencv-python==5.0.0.93
numpy==2.2.6
sounddevice==0.5.5
soundfile==0.14.0

# ── Transport encryption ──────────────────────────────────────────────────
cryptography==49.0.0

# ── Web streaming ─────────────────────────────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.8
