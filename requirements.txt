# Megaploit requirements
# Install: pip install -r requirements.txt
#
# Notes:
#   PyAudio needs PortAudio:
#     Linux:   sudo apt-get install portaudio19-dev
#     macOS:   brew install portaudio
#     Windows: pre-built wheel at https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
#
#   readline is a stdlib module on Linux/macOS (tab completion built-in).
#   On Windows install pyreadline3:
#     pip install pyreadline3

# Core (server + agent)
termcolor>=2.3.0
pynput>=1.7.6

# Screenshot / GUI automation (agent)
PyAutoGUI>=0.9.54

# Screen capture — desktop streaming (agent)
mss>=9.0.1

# Image processing / webcam streaming (agent)
opencv-python>=4.8.0
numpy>=1.24.0

# Audio recording (agent)
PyAudio>=0.2.14

# Web streaming Flask servers (agent)
Flask>=3.0.0
Werkzeug>=3.0.0
